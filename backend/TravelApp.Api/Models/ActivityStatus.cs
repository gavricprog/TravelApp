namespace TravelApp.Api.Models;

/// <summary>Simple lifecycle for an activity — kept as enum for clear API values.</summary>
public enum ActivityStatus
{
    Planned = 0,
    Done = 1,
    Cancelled = 2
}
